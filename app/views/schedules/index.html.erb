<h1>Listado de horarios disponibles</h1>

<table>
  <thead>
    <tr>
      <th>Horario</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @schedules.each do |schedule| %>
      <tr>
        <td><%= schedule.name %></td>
        <td><%= link_to 'Mostrar', schedule %></td>
        <td><%= link_to 'Editar', edit_schedule_path(schedule) %></td>
        <td><%= link_to 'Borrar', schedule, method: :delete, data: { confirm: '¿Borrar horario?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo horario', new_schedule_path %>
