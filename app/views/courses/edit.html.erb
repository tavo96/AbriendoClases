<% if userIsCoordinator %>
	<h1>Editando clase</h1>

	<%= render 'form' %>

	<%= link_to 'Mostrar', @course %> |
	<%= link_to 'Regresar', courses_path %>
<% else %>
	<%= controller.redirect_to requests_path %>
<% end %>
